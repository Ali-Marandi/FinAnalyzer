# اسکریپت کامل ارائه امنیتی هیئت‌مدیره — FinAnalyzer v2.3.0

**موضوع:** هویت سازمانی، MFA، SSO و اعتبارسنجی امنیتی  
**مدت پیشنهادی:** ۱۲ تا ۱۴ دقیقه، به‌علاوه ۸ دقیقه پرسش‌وپاسخ  
**مخاطب:** هیئت‌مدیره، مدیرعامل، مدیر مالی، مدیر فناوری، مدیر ریسک و امنیت  
**هدف جلسه:** تصویب Pilot کنترل‌شده v2.3.0 و تأمین تکمیل‌های لازم برای استقرار سازمانی حساس.

> **اصل صداقت در ارائه:** v2.3.0 یک پیاده‌سازی مرجع و پایه امنیتی است. Microsoft Entra/OIDC، MSAL، principal نشست، کنترل MFA age و تست‌های محلی به کد افزوده شده‌اند. اما tenant مشتری، Conditional Access، authentication context، group provisioning/SCIM، service account غیرانسانی، Pentest مستقل و Plaid Production هنوز کار استقرار و assurance هستند؛ نباید آن‌ها را «تکمیل‌شده» معرفی کرد.

## نقشه زمان‌بندی

| اسلاید | زمان | پیام اصلی |
| --- | ---: | --- |
| 1. جلد | 0:00–0:40 | v2.3.0 هویت سازمانی را به کنترل مالی متصل می‌کند. |
| 2. از v2.2 به v2.3 | 0:40–1:55 | RBAC و DPAPI اکنون به هویت و MFA واقعی متصل می‌شوند. |
| 3. مسئله هویت در Enterprise | 1:55–3:15 | actor ID و flag داخلی جایگزین هویت و session قابل‌اعتماد نمی‌شوند. |
| 4. معماری SSO | 3:15–5:05 | Entra/OIDC + PKCE + MSAL و principal محلی، مسیر اعتماد را می‌سازند. |
| 5. MFA و Step-up | 5:05–6:35 | سطح احراز هویت بر مبنای عمل مالی افزایش می‌یابد. |
| 6. کنترل سرویس‌محور | 6:35–8:05 | Plaid و گزارش‌ها فقط principal معتبر می‌پذیرند. |
| 7. آزمون و تاب‌آوری | 8:05–9:45 | تست‌های داخلی انجام شده‌اند؛ Pentest مستقل همچنان گام ضروری است. |
| 8. استقرار Pilot | 9:45–11:15 | مسیر Entra test tenant تا Pilot محدود و کنترل‌شده است. |
| 9. تصمیم‌های هیئت‌مدیره | 11:15–12:30 | سه تصمیم برای خروج از پایه فنی و ورود به assurance سازمانی. |

---

## اسلاید 1 — FinAnalyzer Enterprise v2.3.0

**زمان پیشنهادی:** ۴۰ ثانیه

### متن گفتاری دقیق

«امروز درباره یک ویژگی جدید در رابط کاربری صحبت نمی‌کنیم؛ درباره انتقال FinAnalyzer از یک کنترل دسترسی داخلی به یک زنجیره قابل‌اعتماد هویت سازمانی، MFA و مجوزدهی برای عملیات مالی صحبت می‌کنیم.

در v2.3.0، ما نمونه اجرایی ورود سازمانی با Microsoft Entra، OIDC و MSAL را به هسته کنترل دسترسی قبلی متصل کرده‌ایم. به‌جای اینکه یک صفحه یا script بگوید “این کاربر MFA شده است”، وضعیت هویت و MFA از نشست ایجادشده پس از احراز هویت معتبر به دست می‌آید. هدف جلسه این است که روشن کنیم چه چیزی پیاده شده، چه ریسک‌هایی کاهش یافته، چه مواردی هنوز باز است و چه تصمیمی از هیئت‌مدیره نیاز داریم.»

### تأکید اجرایی

پس از عبارت «نشست ایجادشده پس از احراز هویت معتبر» مکث کوتاه کنید. این عبارت تفاوت کلیدی میان flag داخلی و context مبتنی بر IdP را نشان می‌دهد.

### انتقال

«ابتدا جایگاه v2.3.0 را در مسیر امنیتی محصول مشخص می‌کنم.»

---

## اسلاید 2 — از v2.2 به v2.3؛ از Policy به Identity

**زمان پیشنهادی:** ۷۵ ثانیه

### متن گفتاری دقیق

«در v2.2.0، سه پایه مهم ساخته شد: RBAC با deny-by-default، تفکیک داده بر اساس شرکت و حفاظت کلید محلی با Windows DPAPI. همچنین Plaid و گزارش‌گیری در سطح سرویس، نه فقط در UI، مجوز صریح را کنترل می‌کردند.

اما یک شکاف مهم باقی می‌ماند. سرویس‌ها هنوز برای بعضی عملیات، actor ID و وضعیت MFA را از caller دریافت می‌کردند. حتی اگر این مقدار از UI عادی برسد، از نگاه معماری Enterprise نمی‌توان آن را منبع نهایی اعتماد دانست.

v2.3.0 این شکاف را هدف گرفته است. اکنون `AuthenticatedPrincipal` از یک ورود فدره معتبر ساخته می‌شود. این principal شامل کاربر محلی، شناسه نشست، issuer و subject خارجی، زمان احراز هویت، زمان MFA و expiry است. سپس `AuthorizationService` همان RBAC و company scope قبلی را بر مبنای این context اعمال می‌کند.

به بیان ساده: v2.2.0 تصمیم می‌گرفت چه کسی چه کاری می‌تواند انجام دهد؛ v2.3.0 کمک می‌کند اطمینان پیدا کنیم آن «چه کسی» از مسیر هویتی قابل‌اعتماد آمده است.»

### انتقال

«برای درک اهمیت این تغییر، باید مسئله هویت در یک محصول مالی Enterprise را دقیق‌تر ببینیم.»

---

## اسلاید 3 — مسئله هویت در Enterprise

**زمان پیشنهادی:** ۸۰ ثانیه

### متن گفتاری دقیق

«در یک سیستم تحلیل مالی، صرف اینکه یک کاربر شناسه عددی داشته باشد، کافی نیست. باید بدانیم این کاربر از کدام سازمان آمده، چه زمانی احراز هویت کرده، آیا عامل دوم را انجام داده، نشست او هنوز معتبر است و آیا هنوز در همان شرکت و همان نقش قرار دارد.

اگر این زنجیره ناقص باشد، چند ریسک ایجاد می‌شود: یک script می‌تواند flag MFA را جعل کند؛ کاربر پس از revoke ممکن است با context کهنه عمل کند؛ یا یک ایمیل مشابه به‌اشتباه به کاربر محلی متصل شود.

در v2.3.0، شناسه اعتماد برای کاربر خارجی، ترکیب immutable `issuer + subject` است؛ نه ایمیل. ایمیل فقط برای نمایش و سهولت استفاده است. همچنین کاربر جدید Entra به‌صورت خودکار role نمی‌گیرد. اگر external identity هنوز provision نشده باشد، ورود با خطای امن متوقف می‌شود و هیچ دسترسی پیش‌فرض، به‌ویژه admin، داده نمی‌شود.

این رویکرد شاید در ابتدا اصطکاک عملیاتی ایجاد کند، اما از نظر Enterprise صحیح است: آسان‌تر است که دسترسی اضافه بدهیم تا اینکه بعداً دسترسی اشتباهی را از داده مالی حساس پس بگیریم.»

### نکته برای پرسش احتمالی

اگر پرسیده شد «چرا JIT provisioning باز نیست؟» پاسخ دهید: «تا وقتی فرآیند group mapping، تأیید مدیر و lifecycle سازمانی کامل نشده، deny-by-default از ساخت خودکار حساب ایمن‌تر است.»

### انتقال

«اکنون مسیر فنی اعتماد، از مرورگر کاربر تا اجرای عملیات در سرویس را مرور می‌کنیم.»

---

## اسلاید 4 — معماری SSO با Entra، OIDC و PKCE

**زمان پیشنهادی:** ۱۱۰ ثانیه

### متن گفتاری دقیق

«در سمت کاربر، برنامه دسکتاپ FinAnalyzer یک public client است. به این معنا که نباید client secret یا credential سازمان در فایل اجرایی وجود داشته باشد. ورود تعاملی با MSAL انجام می‌شود و مرورگر سیستمی، صفحه Microsoft Entra را باز می‌کند.

مسیر استاندارد، Authorization Code همراه با PKCE و OIDC است. PKCE کمک می‌کند کدی که از مرورگر بازمی‌گردد به همان session شروع‌کننده وابسته باشد. در callback، token برای issuer، audience، امضا، tenant و expiry اعتبارسنجی می‌شود. سپس فقط اگر subject خارجی از قبل به کاربر محلی فعال bind شده باشد، یک AuthSession محلی ایجاد می‌شود.

این نشست محلی کوتاه‌عمر است و در دیتابیس تنها metadata لازم برای کنترل و audit نگهداری می‌شود. MSAL token cache در Windows فقط با DPAPI ذخیره می‌شود؛ در محیط غیر-Windows، cache ماندگار خام ایجاد نمی‌کنیم تا refresh token در فایل بدون حفاظت باقی نماند.

نکته مهم این است که Microsoft Entra نقش Identity Provider را دارد. اما permission نهایی را Entra به‌تنهایی تعیین نمی‌کند. Entra به ما می‌گوید کاربر چه کسی است و چه سطح احراز هویتی داشته است؛ AuthorizationService محلی تعیین می‌کند آیا آن کاربر در شرکت مشخص اجازه اتصال بانک، گزارش یا ثبت دفتر کل دارد یا نه.»

### تأکید اجرایی

با دست روی بخش «Identity Provider» و سپس «Authorization Service» اشاره کنید و بگویید: «هویت بیرون از برنامه تأیید می‌شود؛ مجوز مالی داخل مرز محصول enforce می‌شود.»

### انتقال

«با داشتن هویت معتبر، حالا می‌توانیم MFA را متناسب با ریسک عمل مالی به کار بگیریم.»

---

## اسلاید 5 — MFA و Step-up بر مبنای ریسک عمل

**زمان پیشنهادی:** ۹۰ ثانیه

### متن گفتاری دقیق

«هدف ما این نیست که برای هر مشاهده گزارش، کاربر را با MFA خسته کنیم. هدف، اعمال step-up در نقاطی است که ریسک مالی یا خروج داده افزایش می‌یابد.

برای مشاهده گزارش داخلی یا sync عادی، principal معتبر و permission مناسب لازم است. اما برای Link یا Unlink بانک، تغییر نقش، ارسال خارجی گزارش و ثبت حساس دفتر کل، علاوه بر permission، شواهد MFA باید تازه باشد. در نمونه v2.3.0، حداکثر سن MFA قابل تنظیم است و به‌صورت پیش‌فرض ۱۵ دقیقه در نظر گرفته شده است.

اگر MFA قدیمی یا غایب باشد، سرویس نباید صرفاً به خاطر اینکه کاربر قبلاً وارد شده عمل حساس را انجام دهد. باید step-up آغاز شود یا درخواست با خطای امن رد گردد. این policy در context تولیدشده از principal اعمال می‌شود؛ بنابراین caller نمی‌تواند با ارسال `mfa_verified=True` آن را دور بزند.

برای محیط Enterprise حساس، روش‌های مقاوم‌تر در برابر فیشینگ مانند FIDO2، passkey یا Windows Hello for Business باید در Conditional Access یا authentication strength مشتری تنظیم شوند. TOTP می‌تواند fallback باشد، اما برای عملیات پرریسک نباید تنها مسیر مورد اتکا تلقی شود.»

### انتقال

«این principal و MFA policy اکنون به جای actor خام، به دو جریان حساس محصول تزریق شده‌اند.»

---

## اسلاید 6 — کنترل سرویس‌محور برای Plaid و گزارش‌ها

**زمان پیشنهادی:** ۹۰ ثانیه

### متن گفتاری دقیق

«در v2.3.0، عملیات Plaid شامل ایجاد Link token، exchange public token، sync و unlink، دیگر actor ID و MFA flag قابل‌تغییر نمی‌پذیرند. هر کدام یک AuthenticatedPrincipal می‌خواهند. سرویس از همان principal یک AuthorizationContext می‌سازد و سپس RBAC، company scope و تازگی MFA را کنترل می‌کند.

همین الگو در گزارش‌گیری اعمال شده است. تولید گزارش، ایجاد schedule، اجرای schedule و delivery بیرونی نیازمند principal هستند. برای runner زمان‌بندی‌شده نیز actor ID خام کنار گذاشته شده است. اکنون runner فقط یک session ID معتبر می‌پذیرد و اگر session وجود نداشته باشد، revoke شده باشد یا expire شده باشد، fail-closed می‌شود.

این تغییر یک پیام عملیاتی دارد: تا وقتی service account غیرانسانی و lifecycle آن طراحی نشده، نباید با حساب مدیر یا boolean ساختگی، schedule حساس را به اجبار فعال کنیم. ممکن است این محدودیت کوتاه‌مدت ایجاد کند، اما از ایجاد یک bypass دائمی در لایه مالی جلوگیری می‌کند.

در نهایت، هر درخواست ردشده یا grant حساس در audit trail ثبت می‌شود تا پاسخ به سؤال «چه کسی، چه زمانی، در کدام شرکت و با چه contextی این عمل را انجام داده» قابل پیگیری باشد.»

### انتقال

«اکنون به جای اعتماد صرف به کد، شواهد آزمون و مرزهای باقی‌مانده را بررسی می‌کنیم.»

---

## اسلاید 7 — آزمون، تست نفوذ و تاب‌آوری

**زمان پیشنهادی:** ۱۰۰ ثانیه

### متن گفتاری دقیق

«برای v2.3.0، سیزده آزمون محلی مربوط به OIDC/MFA، principal، RBAC، DPAPI، Plaid و گزارش اجرا شده‌اند. سناریوهای پوشش‌داده‌شده شامل token منقضی، کاربر provision نشده، MFA قدیمی، logout و revoke نشست، تلاش برای ارسال actor یا MFA flag خام به سرویس، tenant scope و sync/report مجاز هستند.

همچنین یک runner امنیتی اضافه شده است که با یک فرمان، regression testهای محلی را اجرا می‌کند و می‌تواند Bandit، dependency audit و secret scan را نیز در CI فعال کند. این runner عمداً به Microsoft Entra، Plaid، بانک یا شبکه مشتری حمله نمی‌کند. تست‌های شبکه‌ای و Pentest فقط با مجوز کتبی، محیط ایزوله، test tenant و Rules of Engagement مستقل انجام می‌شوند.

در dependency audit محیط sandbox، چهار آسیب‌پذیری در سه بسته محیط پایه دیده شد: دو مورد در pypdf با نسخه رفع‌شده، یک مورد در wheel با نسخه رفع‌شده و یک مورد در xhtml2pdf بدون fix ثبت‌شده. این یافته‌ها به معنی exploit در FinAnalyzer نیستند، اما پیش از build نهایی ویندوز باید با upgrade، حذف dependency یا پذیرش ریسک رسمی مدیریت شوند.

بنابراین، وضعیت assurance این است: کنترل‌های پایه و regression test ساخته شده‌اند، اما Pentest مستقل و remediation وابستگی‌ها هنوز gate انتشار سازمانی حساس هستند.»

### مکث و تأکید

در جمله «این runner عمداً حمله نمی‌کند» مکث کنید. هدف، جلوگیری از این برداشت است که اسکن داخلی معادل Pentest مستقل است.

### انتقال

«برای تبدیل این پایه فنی به یک Pilot سازمانی، مسیر استقرار را کوتاه و کنترل‌شده نگه می‌داریم.»

---

## اسلاید 8 — مسیر Pilot سازمانی

**زمان پیشنهادی:** ۹۰ ثانیه

### متن گفتاری دقیق

«Pilot را در چهار گام اجرا می‌کنیم. گام اول، Microsoft Entra test tenant و App Registration است: redirect URI loopback، public client، گروه آزمایشی و Conditional Access مناسب.

گام دوم، provisioning محدود چند کاربر و نقش در یک شرکت آزمایشی است. هیچ کاربری به‌صورت خودکار administrator نمی‌شود. identity خارجی به user محلی bind می‌شود و membership/role در شرکت مشخص اعطا می‌گردد.

گام سوم، آزمایش flow واقعی است: ورود با SSO، MFA، عملیات گزارش داخلی، Link Plaid در Sandbox، sync با داده ساختگی، logout، revoke و error handling. همزمان logs و audit trail بازبینی می‌شوند تا مطمئن شویم secret یا token وارد output نشده است.

گام چهارم، بازبینی مستقل است: تحلیل وابستگی‌ها، Pentest مجاز، تست پایداری هنگام outage IdP، بررسی DPAPI روی Windows build تمیز و تصمیم درباره residual risk. تنها پس از عبور از این گام، درباره مشتری حساس یا Plaid Production بحث می‌کنیم.

این مدل Pilot، ریسک را محدود می‌کند و به ما اجازه می‌دهد هم تجربه کاربر و هم کنترل امنیتی را با شواهد واقعی بسنجیم.»

### انتقال

«در پایان، سه تصمیم عملی نیاز داریم تا این مسیر از prototype به قابلیت Enterprise قابل‌عرضه تبدیل شود.»

---

## اسلاید 9 — تصمیم‌های پیشنهادی هیئت‌مدیره

**زمان پیشنهادی:** ۷۵ ثانیه

### متن گفتاری دقیق

«تصمیم اول، تأیید v2.3.0 به‌عنوان پایه Identity و Security محصول است؛ با این شرط که capabilityهای جدید مالی فقط از principal و AuthorizationService سرویس‌محور استفاده کنند و مسیر raw actor/MFA flag دوباره ایجاد نشود.

تصمیم دوم، تأمین مالک اجرایی و بودجه Pilot سازمانی است. این شامل Entra test tenant، Conditional Access، MFA مقاوم‌تر در برابر فیشینگ، provisioning کنترل‌شده، service account design و تکمیل group-to-role mapping است.

تصمیم سوم، تعریف gate مستقل برای استقرار حساس است: remediation یا risk acceptance وابستگی‌ها، Pentest مصوب، retest یافته‌ها، بررسی build Windows و بازبینی release policy قبل از هر استفاده از Plaid Production یا داده مالی واقعی.

درخواست ما این است که این سه تصمیم به‌همراه مسئولیت‌پذیری روشن تصویب شود. با این کار، FinAnalyzer فقط مجموعه‌ای از قابلیت‌های مالی نخواهد بود؛ بلکه پلتفرمی خواهد بود که هویت، مجوز و عملیات حساس را به‌صورت قابل‌کنترل و قابل‌حسابرسی مدیریت می‌کند.

سپاسگزارم. آماده‌ام درباره Pilot، ریسک باقی‌مانده، هزینه assurance و ترتیب اولویت‌ها پاسخ بدهم.»

---

# پاسخ‌های پیشنهادی به پرسش‌های هیئت‌مدیره

| پرسش | پاسخ پیشنهادی |
| --- | --- |
| آیا اکنون همه کاربران می‌توانند با SSO وارد شوند؟ | «کد و الگوی Entra آماده است، اما هر سازمان باید App Registration، tenant policy و provisioning کاربران خود را در محیط کنترل‌شده پیکربندی کند.» |
| آیا MFA در محصول کامل است؟ | «محصول اکنون MFA evidence را از principal معتبر مصرف و age آن را کنترل می‌کند. اما روش‌های واقعی factor و Conditional Access در IdP مشتری پیکربندی می‌شوند و باید در Pilot آزمون شوند.» |
| چرا client secret نداریم؟ | «برنامه دسکتاپ public client است؛ secret در EXE قابل حفاظت مطمئن نیست. جریان PKCE برای همین نوع client طراحی شده است.» |
| آیا dependency audit شکست خورده است؟ | «Regression tests و Bandit پاس شدند، اما audit محیط sandbox چهار یافته dependency گزارش کرد. این‌ها gate مشخص قبل از build نهایی هستند و باید رفع یا به‌طور رسمی پذیرفته شوند.» |
| آیا scheduler اکنون برای ارسال بیرونی آماده است؟ | «برای جلوگیری از bypass، scheduler بدون session معتبر fail-closed است. برای production باید مدل service account و policy آن کامل شود.» |
| مهم‌ترین گام بعدی چیست؟ | «Pilot Entra test tenant همراه با MFA policy، سپس Pentest مستقل و remediation یافته‌ها. این ترتیب هم ارزش کسب‌وکاری و هم کاهش ریسک را سریع‌تر نشان می‌دهد.» |

# چک‌لیست پیش از ارائه

پیش از جلسه، commit `47699bc` را به‌عنوان مرجع فنی اعلام کنید و از محیط demo با داده ساختگی استفاده کنید. هیچ token، secret، ایمیل واقعی، بانک واقعی یا screenshot شامل شناسه شخصی را نمایش ندهید. اگر سؤال درباره زمان، بودجه یا انطباق قانونی مطرح شد، تنها مقدارهای تأییدشده توسط مالک اجرایی را بیان کنید. در صورت ابهام، تفاوت میان «پیاده‌سازی کد»، «تست محلی»، «Pilot مشتری» و «تأیید مستقل» را صریح توضیح دهید.
