# نقشه راه RBAC سازمانی و امنیت FinAnalyzer Enterprise

**نسخه مرجع محصول:** v2.1.0  
**هدف سند:** تفکیک وضعیت فعلی از قابلیت‌های موردنیاز برای استقرار Enterprise، و تعریف مسیر اجرایی قابل‌آزمون برای RBAC، احراز هویت، حسابرسی و مدیریت ایمن اسرار.

> **جمع‌بندی مدیریتی:** FinAnalyzer v2.1.0 دارای پایه‌های مفید شامل هش گذرواژه با bcrypt، رمزنگاری Fernet برای توکن Plaid، مدل‌های کاربر/نقش/لاگ حسابرسی و کنترل اولیه نقش‌ها است. با این حال، **RBAC در سطح Enterprise هنوز به‌طور کامل در مرز سرویس‌ها و رابط کاربری enforce نشده است**. به‌ویژه، صفحه اتصال بانکی فعلی مجوز کاربر را بررسی نمی‌کند و مدل داده، عضویت کاربر در شرکت و مجوزهای ریزدانه را ندارد. بنابراین نباید وضعیت فعلی را معادل RBAC سازمانی کامل دانست.

## 1. وضعیت فعلی v2.1.0

| قابلیت موجود | محل پیاده‌سازی | وضعیت و محدودیت |
| --- | --- | --- |
| نقش‌های پایه | `UserRole` در `core/models.py` | سه نقش `admin`، `accountant` و `viewer` تعریف شده‌اند. |
| بررسی سلسله‌مراتبی نقش | `SecurityManager.check_permission()` | مقایسه سطح نقش را انجام می‌دهد، اما به‌صورت سراسری در تمام عملیات سرویس و UI اعمال نشده است. |
| گذرواژه | `SecurityManager.hash_password()` | با bcrypt هش می‌شود؛ نیازمند سیاست نرخ‌دهی، بازیابی، MFA و کنترل چرخه عمر است. |
| لاگ حسابرسی | `AuditLog` و `log_audit()` | ثبت رویداد پایه دارد، ولی ساختار کامل actor، شرکت، شیء هدف، IP/دستگاه، نتیجه و ضد‌دستکاری ندارد. |
| چندشرکتی | `Company` و کلیدهای `company_id` | پایه داده‌ای مناسب برای تفکیک tenant فراهم است، اما عضویت کاربر-شرکت و کنترل سطری وجود ندارد. |
| محدودسازی صفحه بانکی | `ui/pages/banking.py` | در نسخه فعلی پیاده‌سازی نشده؛ این صفحه باید پیش از نمایش و پیش از هر عملیات حساس، مجوز را کنترل کند. |

OWASP تأکید می‌کند که احراز هویت با مجوزدهی متفاوت است، دسترسی باید به‌صورت پیش‌فرض رد شود، و کنترل مجوز باید برای **هر درخواست یا عملیات محافظت‌شده** اعمال شود.[1]

## 2. مدل دسترسی پیشنهادی: RBAC به‌علاوه Scope و ABAC سبک

برای سازمان‌های مالی، یک نقش ساده به‌تنهایی کافی نیست. پیشنهاد عملی این است که **RBAC برای وظایف شغلی** با دو محدودیت تکمیلی ترکیب شود: Scope سازمانی و قواعد ویژگی‌محور سبک. این کار از انفجار نقش‌ها جلوگیری می‌کند و اصل حداقل دسترسی را عملی می‌سازد.[1]

| لایه تصمیم | نمونه | هدف |
| --- | --- | --- |
| هویت | `user_id`، وضعیت فعال، MFA، نوع دستگاه | تعیین اینکه کاربر چه کسی است و آیا نشست معتبر دارد. |
| نقش | مدیر مالی، حسابدار، تحلیلگر، ممیز، ناظر | تعریف مجموعه‌ای از مجوزهای وظیفه‌ای. |
| مجوز | `ledger.post`، `bank.sync`، `report.schedule` | تعریف صریح عمل مجاز؛ نه اتکا به نام نقش در UI. |
| Scope | `company_id`، واحد کسب‌وکار، حساب بانکی، دوره مالی | جلوگیری از دسترسی افقی به داده شرکت یا واحد دیگر. |
| شرط ویژگی‌محور | MFA انجام شده، دوره مالی باز است، اتصال از دستگاه تأییدشده است | اعمال کنترل پویا برای عملیات حساس. |

### نقش‌های پیشنهادی اولیه

| نقش | هدف | نمونه مجوزهای اصلی | محدودیت مهم |
| --- | --- | --- | --- |
| `platform_admin` | مدیریت استقرار و سیاست‌های کل سیستم | پیکربندی SSO، کلیدها، tenantها، ممیزی | نباید به‌صورت پیش‌فرض به داده مالی همه شرکت‌ها دسترسی خواندن داشته باشد. |
| `company_admin` | مدیریت کاربران و تنظیمات یک شرکت | عضویت‌ها، نقش‌ها، سیاست گزارش، اتصال بانکی | نیازمند MFA برای تغییر نقش یا حذف اتصال بانکی. |
| `finance_manager` | کنترل و تأیید عملیات مالی | تأیید ثبت، بستن دوره، گزارش حساس | فقط در شرکت‌های عضو. |
| `accountant` | ثبت و طبقه‌بندی عملیات | ایجاد/ویرایش پیش‌نویس، تطبیق، گزارش عملیاتی | امکان اعطای نقش یا دسترسی به کلید ندارد. |
| `analyst` | تحلیل و ساخت گزارش | خواندن داده مجاز، ساخت گزارش، سناریو | عملیات ثبت یا همگام‌سازی بانکی ندارد. |
| `auditor` | مشاهده و ممیزی | خواندن دفتر کل، لاگ، گزارش‌های قفل‌شده | کاملاً فقط‌خواندنی؛ خروجی حساس نیازمند policy. |
| `viewer` | مشاهده محدود | داشبورد و گزارش‌های منتشرشده | بدون داده خام بانکی، بدون تغییر. |
| `bank_operator` | اتصال و همگام‌سازی بانکی | `bank.link`، `bank.sync` | تفکیک وظیفه از تأیید نهایی حسابداری. |

### فرهنگ مجوز پیشنهادی

مجوزها باید رشته‌های پایدار، قابل‌آزمون و مستقل از رابط کاربری باشند. نمونه‌ها عبارت‌اند از:

```text
company.read                 company.members.manage
identity.role.assign         identity.session.revoke
ledger.read                  ledger.draft.create
ledger.entry.post            ledger.entry.void
period.close                 account.manage
bank.link                    bank.sync
bank.unlink                  bank.account.map
report.generate              report.schedule.manage
report.deliver.external      audit.read
security.key.rotate          integration.configure
```

## 3. تغییرات پیشنهادی در مدل داده

مدل فعلی `User.role` برای شروع ساده مناسب است، اما برای Enterprise باید به مدل many-to-many و scoped تبدیل شود.

| جدول پیشنهادی | ستون‌های حیاتی | کاربرد |
| --- | --- | --- |
| `roles` | `id`, `code`, `name`, `is_system` | نقش‌های سیستمی و نقش‌های سفارشی سازمان. |
| `permissions` | `id`, `code`, `resource`, `action` | فهرست یکتای مجوزهای محصول. |
| `role_permissions` | `role_id`, `permission_id` | نگاشت نقش به مجوز. |
| `company_memberships` | `user_id`, `company_id`, `status`, `joined_at` | ایجاد مرز tenant و عضویت رسمی کاربر در شرکت. |
| `membership_roles` | `membership_id`, `role_id` | نقش کاربر در یک شرکت، نه در کل سامانه. |
| `service_accounts` | `company_id`, `name`, `secret_ref`, `scopes`, `expires_at` | اجرای زمان‌بندی‌شده و یکپارچه‌سازی‌ها بدون استفاده از حساب انسانی. |
| `auth_sessions` | `user_id`, `issued_at`, `expires_at`, `mfa_at`, `revoked_at`, `device_id` | مدیریت نشست، خروج اجباری و ممیزی نشست‌ها. |
| `audit_events` | `actor`, `company_id`, `action`, `target_type`, `target_id`, `result`, `metadata_hash`, `occurred_at` | حسابرسی ساختاریافته و قابل‌جست‌وجو. |

هر منبع مالیِ tenant-scoped، شامل `JournalEntry`، `Account`، `PlaidItem`، گزارش زمان‌بندی‌شده و فایل خروجی، باید در کوئری سرویس با `company_id` عضویت کاربر فیلتر شود. شناسه قابل‌حدس یا فقط پنهان‌کردن یک دکمه در UI نباید مبنای مجوز باشد.[1]

## 4. الگوی پیاده‌سازی در کد

### 4.1 لایه Policy مرکزی

یک سرویس مرکزی مانند `AuthorizationService` ایجاد کنید. هیچ صفحه PySide6، job زمان‌بندی‌شده، endpoint آینده یا سرویس دامنه نباید نقش را مستقیم مقایسه کند.

```python
class AuthorizationService:
    def require(self, actor, permission: str, company_id: int, context: dict | None = None) -> None:
        if not actor.is_active:
            raise AuthorizationDenied("Inactive user")
        if not self.has_permission(actor.id, permission, company_id, context or {}):
            self.audit_denial(actor.id, company_id, permission, context or {})
            raise AuthorizationDenied("Access denied")
```

قواعد کلیدی عبارت‌اند از **deny-by-default**، کنترل هم‌زمان `company_id` و مجوز، و ثبت هم موفقیت و هم ردشدن عملیات حساس. کنترل باید در سرویس دامنه برقرار باشد؛ UI فقط برای تجربه کاربری آن را بازتاب دهد. OWASP توصیه می‌کند کنترل مجوز برای هر عملیات محافظت‌شده معتبرسازی و با تست واحد/یکپارچه پوشش داده شود.[1]

### 4.2 نقاط اعمال مجوز در FinAnalyzer

| عملیات | مجوز لازم | کنترل اضافی |
| --- | --- | --- |
| مشاهده گزارش | `report.generate` یا `ledger.read` | Scope شرکت و محدودیت دوره مالی. |
| ایجاد یا ویرایش پیش‌نویس ثبت | `ledger.draft.create` | Scope شرکت و حساب‌های مجاز. |
| ثبت نهایی یا ابطال ثبت | `ledger.entry.post` / `ledger.entry.void` | MFA تازه، دوره باز، و در صورت نیاز maker-checker. |
| اتصال Plaid | `bank.link` | MFA تازه، شرکت فعال، ثبت رویداد و تأیید کاربر. |
| همگام‌سازی Plaid | `bank.sync` یا service account scoped | Scope همان `PlaidItem.company_id`. |
| قطع اتصال بانکی | `bank.unlink` | MFA تازه و ثبت actor/reason. |
| زمان‌بندی یا ارسال خارجی گزارش | `report.schedule.manage` / `report.deliver.external` | فهرست گیرندگان مجاز و تأیید دومرحله‌ای در محیط‌های حساس. |
| مدیریت نقش | `identity.role.assign` | جداسازی وظیفه؛ مدیر نباید نقش بالاتر از نقش خود اعطا کند. |

### 4.3 احراز هویت و نشست

برای نسخه دسکتاپ Enterprise، گزینه‌های اجرا را بر اساس محیط مشتری انتخاب کنید.

| گزینه | مناسب برای | توصیه |
| --- | --- | --- |
| حساب محلی + bcrypt | نسخه تک‌کاربره یا آفلاین کنترل‌شده | فقط شروع کار؛ نیازمند قفل حساب، rate limit، MFA و مدیریت بازیابی است. |
| OIDC/OAuth2 با IdP سازمانی | اغلب مشتریان سازمانی | گزینه پیش‌فرض پیشنهادی؛ پشتیبانی از MFA، lifecycle کاربر و خروج متمرکز. |
| SAML 2.0 / Entra ID / Okta | سازمان‌های دارای SSO | برای فروش Enterprise ضروری و با provisioning خودکار قابل‌تکمیل است. |
| Windows Integrated Authentication | محیط‌های کاملاً Windows/AD | در صورت نیاز مشتری، به‌عنوان اتصال‌دهنده سازمانی جداگانه. |

در هر گزینه، نشست باید کوتاه‌عمر، قابل‌ابطال و قابل‌ممیزی باشد. پس از ارتقای سطح دسترسی، تغییر نقش، تغییر رمز، یا اتصال/حذف بانک، نشست باید بازتولید یا کاربر دوباره احراز هویت شود. OWASP بر نگهداری امن شناسه نشست، زمان انقضا، بازتولید نشست پس از تغییر سطح دسترسی و ثبت چرخه عمر نشست تأکید می‌کند.[2]

### 4.4 تفکیک وظیفه و چهارچشمی

برای عملیات پرریسک مالی، نقش‌ها را فقط به «اجازه» محدود نکنید. نمونه سیاست‌ها عبارت‌اند از: شخصی که اتصال بانکی را فعال می‌کند نمی‌تواند به‌تنهایی تغییرات حساب‌های مقصد را تأیید کند؛ ایجادکننده ثبت نمی‌تواند همان ثبت بالاتر از آستانه را تأیید نهایی کند؛ و تغییر گیرنده گزارش خارجی نیازمند تأیید دومین مدیر است. این سیاست‌ها باید در مدل workflow و audit ثبت شوند، نه صرفاً در دستورالعمل سازمانی.

## 5. جزئیات فنی رمزنگاری محلی و توکن Plaid در v2.1.0

### 5.1 واقعیت پیاده‌سازی موجود

`LocalSecretStore` در `core/security.py` یک کلید Fernet را به یکی از دو روش تأمین می‌کند:

1. اگر متغیر محیطی `FINANALYZER_MASTER_KEY` وجود داشته باشد، همان کلید برای ایجاد cipher استفاده می‌شود.
2. در غیر این صورت، برنامه کلید را از `data/.finanalyzer.key` می‌خواند یا یک کلید Fernet جدید ایجاد می‌کند. در سیستم‌های شبه‌یونیکس تلاش می‌شود سطح دسترسی فایل `0600` تنظیم شود؛ در Windows این کنترل به ACL پروفایل کاربر واگذار شده است.

سپس `encrypt()` یک رشته حساس را رمز می‌کند و `decrypt()` فقط در زمان لازم، مانند فراخوانی `Transactions Sync` یا حذف Item، مقدار را در حافظه بازمی‌گرداند. در دیتابیس، ستون `plaid_items.encrypted_access_token` فقط ciphertext را نگه می‌دارد؛ کلید در این جدول ذخیره نمی‌شود.

> **نکته حیاتی:** `SecurityManager` نیز یک Fernet cipher دارد، اما اگر بدون کلید ورودی ساخته شود، کلید موقتی تولید می‌کند. برای اسرار پایدار مانند توکن Plaid، مسیر درست در نسخه فعلی `LocalSecretStore` است، نه نمونه پیش‌فرض `SecurityManager`.

### 5.2 چرخه کامل Plaid

| مرحله | رفتار v2.1.0 | داده پایدار |
| --- | --- | --- |
| پیکربندی | `PLAID_CLIENT_ID`، `PLAID_SECRET` و `PLAID_ENV` از environment خوانده می‌شوند. | هیچ secret در سورس یا دیتابیس برنامه ذخیره نمی‌شود. |
| آغاز Link | `create_link_token(company_id)` یک `link_token` کوتاه‌عمر تولید می‌کند؛ شناسه شرکت استفاده می‌شود و PII ارسال نمی‌شود. | Link token ذخیره نمی‌شود. |
| رضایت کاربر | `PlaidDesktopLinkBridge` فقط روی `127.0.0.1` سرور موقت اجرا و مرورگر پیش‌فرض را باز می‌کند. | `public_token` فقط در همان گردش کوتاه‌عمر استفاده می‌شود. |
| تبادل | `item_public_token_exchange`، `public_token` را به `access_token` و `item_id` تبدیل می‌کند. | `item_id` و ciphertext توکن ذخیره می‌شوند؛ public token ذخیره نمی‌شود. |
| ذخیره‌سازی | `LocalSecretStore.encrypt(access_token)` پیش از درج در `PlaidItem` اجرا می‌شود. | `encrypted_access_token`، cursor، شناسه/نام مؤسسه و وضعیت. |
| همگام‌سازی | توکن فقط در حافظه decrypt و برای `transactions_sync` استفاده می‌شود؛ شمارش صفحه ۵۰۰ است. | cursor تنها پس از اتمام موفق تمام صفحات به‌روزرسانی می‌شود. |
| اعمال تغییر | داده‌های `added`، `modified` و `removed` به حساب‌های محلی، ثبت‌های دوطرفه و `PlaidTransactionMapping` نگاشت می‌شوند. | شناسه تراکنش ارائه‌دهنده، ارتباط با ثبت حسابداری، payload حسابرسی. |
| لغو اتصال | نخست `/item/remove` از Plaid فراخوانی می‌شود؛ فقط بعد از موفقیت، Item محلی حذف می‌شود. | داده محلی در صورت خطای حذف راه دور حفظ می‌شود. |

این الگو با راهنمای Plaid هم‌راستاست: Link token کوتاه‌عمر است، public token یک‌بارمصرف است و نیازی به ذخیره ندارد، access token باید در datastore امن نگهداری شود و هرگز در کد سمت کاربر قرار نگیرد.[3] راهنمای رسمی Plaid همچنین توصیه می‌کند cursor فقط پس از پردازش موفق کل صفحات همگام‌سازی ذخیره شود؛ در صورت خطا هنگام صفحه‌بندی، روند از ابتدا شروع شود.[3]

### 5.3 حفاظت‌های فعلی

| حفاظت | نحوه اجرا |
| --- | --- |
| عدم hard-code اسرار | تنظیمات Plaid و ارسال گزارش از environment خوانده می‌شوند. |
| رمزنگاری token در حالت سکون | دسترسی Plaid قبل از ذخیره‌سازی با Fernet رمز می‌شود. |
| حذف public token | پس از تبادل ذخیره نمی‌شود. |
| عدم ثبت جزئیات مرورگر | bridge، `log_message` را خاموش می‌کند تا جریان بانک در log عمومی نیاید. |
| جلوگیری از cursor نیمه‌کاره | cursor فقط بعد از موفقیت تمام صفحات sync ثبت می‌شود. |
| provenance | `PlaidTransactionMapping` مانع ثبت تکراری و مسیر منبع تا دفتر کل را نگه می‌دارد. |
| خروج امن | در خطای حذف راه دور، داده محلی حذف نمی‌شود. |

### 5.4 شکاف‌های باقی‌مانده برای Enterprise

رمزنگاری فایل‌محور محلی برای نسخه local-first مناسب است، اما برای استقرار Enterprise با ریسک بالا باید ارتقا یابد.

| اولویت | بهبود پیشنهادی | دلیل |
| --- | --- | --- |
| بحرانی | استفاده از Windows DPAPI/Credential Manager یا KMS/HSM سازمانی به جای فایل کلید ساده | حفاظت کلید با هویت سیستم‌عامل یا سرویس کلید مرکزی. |
| بحرانی | نسخه‌گذاری کلید (`key_id`)، rotation، re-encryption و فرآیند بازیابی کنترل‌شده | امکان چرخش کلید بدون از دست‌دادن توکن‌ها. |
| بالا | رمزنگاری کامل دیسک/پروفایل و در صورت نیاز رمزنگاری دیتابیس | کاهش ریسک سرقت فایل SQLite و metadata. |
| بالا | جداسازی کلید، token و DB در مسیرهای جدا و محدودسازی ACL در نصب‌کننده Windows | کمینه‌سازی سطح حمله محلی. |
| بالا | ثبت ساختاریافته بدون secret و redaction اجباری | جلوگیری از نشت accidental در log، crash report یا telemetry. |
| بالا | MFA و re-authentication برای `bank.link`، `bank.unlink` و export/delivery حساس | جلوگیری از سوءاستفاده از نشست کاربر باز. |
| متوسط | webhook امضاشده و صف retry با idempotency | خودکارسازی امن sync در محیط اتصال‌پذیر. |
| متوسط | DLP برای خروجی PDF/Excel و policy گیرنده | کنترل خروج داده مالی از سازمان. |

## 6. برنامه اجرایی پیشنهادی

| فاز | خروجی قابل‌تحویل | معیار پذیرش |
| --- | --- | --- |
| 1 — پایه RBAC | جداول membership/role/permission، `AuthorizationService`، deny-by-default | تست منفی هر مجوز؛ کاربر شرکت A به داده شرکت B دسترسی ندارد. |
| 2 — اعمال در سرویس | کنترل مجوز در connector Plaid، گزارش، accounting engine و job scheduler | UI bypass هیچ عملیات حساس را ممکن نمی‌کند. |
| 3 — Enterprise Identity | OIDC/SAML، MFA، session lifecycle، provisioning/deprovisioning | خروج کاربر غیرفعال فوری و ثبت‌شده؛ MFA برای عملیات حساس. |
| 4 — Audit و SoD | audit event ساختاریافته، maker-checker، خروجی ممیزی | گزارش کامل actor/action/target/result قابل‌تهیه است. |
| 5 — Key management | DPAPI/KMS، rotation، backup/recovery | چرخش کلید بدون قطع sync یا از دست‌رفتن token. |
| 6 — Assurance | threat model، تست نفوذ، SAST/DAST، dependency scanning و آزمون regression مجوز | یافته‌های بحرانی بسته و تست‌های مجوز در CI اجباری‌اند. |

## 7. تعریف Done برای Enterprise RBAC

یک قابلیت امنیتی زمانی «تمام‌شده» محسوب می‌شود که UI، سرویس دامنه، job زمان‌بندی‌شده و هر API آینده همگی از همان policy مرکزی استفاده کنند؛ داده tenant دیگر با تغییر شناسه قابل مشاهده نباشد؛ مسیرهای ردشده audit شوند؛ تست‌های مجوز در CI اجرا شوند؛ و مدیر بتواند عضویت، نقش، نشست و خروج کاربر را به‌صورت قابل‌ممیزی مدیریت کند. صرف وجود enum نقش یا پنهان‌سازی دکمه در UI، RBAC Enterprise محسوب نمی‌شود.

## منابع

[1]: https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html "OWASP Authorization Cheat Sheet"
[2]: https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html "OWASP Session Management Cheat Sheet"
[3]: https://plaid.com/docs/transactions/add-to-app/ "Plaid — Add Transactions to Your App"
