# ارائه هیئت‌مدیره — امنیت سازمانی FinAnalyzer v2.2.0

## Cover

**FinAnalyzer Enterprise v2.2.0**

امنیت سازمانی، کنترل دسترسی و حفاظت از داده‌های مالی

ارائه برای هیئت‌مدیره

## Slide 1

**چرا این ارتقا اکنون ضروری است**

- داده مالی، اتصال بانکی و گزارش مدیریتی در یک محیط عملیاتی به هم می‌رسند.
- کنترل صرفاً در رابط کاربری، در برابر اجرای مستقیم سرویس یا job زمان‌بندی‌شده کافی نیست.
- نسخه جدید، مرزهای هویت، شرکت و مجوز را در لایه سرویس اعمال می‌کند.

**پیام هیئت‌مدیره:** امنیت از «قابلیت جانبی» به «پیش‌شرط فروش سازمانی» تبدیل شده است.

## Slide 2

**رد پیش‌فرض؛ مجوز فقط با اثبات صریح**

- هر عمل محافظت‌شده به کاربر فعال، عضویت معتبر در شرکت و permission صریح نیاز دارد.
- نقش‌ها از مجوزها جدا شده‌اند؛ نقش‌ها در محدوده هر شرکت واگذار می‌شوند.
- درخواست نامعتبر یا خارج از محدوده شرکت رد و در audit trail ثبت می‌شود.

**نمای عملیاتی:** User → Company Membership → Role → Permission → Approved Service Action

## Slide 3

**کنترل‌های ویژه عملیات مالی حساس**

| عملیات | کنترل اجراشده |
| --- | --- |
| اتصال یا حذف بانک Plaid | مجوز اختصاصی + MFA context |
| همگام‌سازی بانک | مجوز `bank.sync` و محدودیت به شرکت مالک Item |
| تولید گزارش | مجوز `report.generate` در محدوده شرکت |
| زمان‌بندی یا ارسال خارجی گزارش | مجوز حساس + MFA context |

**پیام:** پنهان‌بودن یک دکمه، جایگزین کنترل سرویس نیست.

## Slide 4

**Windows DPAPI؛ حذف فایل کلید خام در ویندوز**

- کلید Fernet محلی اکنون با Windows DPAPI و پروفایل کاربر محافظت می‌شود.
- دسترسی Plaid همچنان فقط به‌صورت ciphertext در SQLite نگهداری می‌شود.
- در خطای DPAPI، سیستم عمداً به فایل کلید خام بازنمی‌گردد.
- مهاجرت یک‌باره، فایل کلید قدیمی را به `.dpapi` منتقل و نسخه خام را حذف می‌کند.

**پیام:** سرقت دیتابیس به‌تنهایی نباید برای استفاده از توکن بانکی کافی باشد.

## Slide 5

**شواهد اجرا و کیفیت کنترل**

- ۷ آزمون یکپارچه با موفقیت اجرا شد.
- کنترل رد پیش‌فرض، جداسازی شرکت، MFA، DPAPI و مهاجرت key پوشش داده شد.
- رمزنگاری توکن Plaid، توازن ثبت‌های دفتر کل و گزارش‌های زمان‌بندی‌شده نیز اعتبارسنجی شدند.

**محدودیت شفاف:** SSO، MFA واقعی، service account و آزمون نفوذ مستقل در نقشه راه تکمیلی هستند.

## Slide 6

**تصمیم‌های پیشنهادی هیئت‌مدیره**

- تصویب v2.2.0 به‌عنوان پایه امنیتی فروش Enterprise.
- اولویت‌دادن به OIDC/SAML، MFA واقعی و service accountهای حداقل‌دسترسی در نسخه بعدی.
- الزام بازبینی امنیتی و تست نفوذ مستقل پیش از استقرار مشتری حساس یا Plaid Production.

**درخواست:** تأیید بودجه و مالکیت اجرایی برای تکمیل IAM سازمانی و assurance مستقل.
