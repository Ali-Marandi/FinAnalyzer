# اسکریپت کامل ارائه امنیتی هیئت‌مدیره

**ارائه:** FinAnalyzer Enterprise v2.2.0 — Enterprise Security Foundation
**تعداد اسلایدهای ارائه فعلی:** ۷ اسلاید
**مدت پیشنهادی:** ۱۰ تا ۱۲ دقیقه، به‌اضافه ۵ تا ۸ دقیقه پرسش‌وپاسخ
**مخاطب:** هیئت‌مدیره، مدیرعامل، مدیر مالی، مدیر فناوری و مسئول ریسک
**هدف:** اخذ تصمیم برای پذیرش v2.2.0 به‌عنوان پایه امنیتی و تأمین حمایت لازم برای تکمیل IAM سازمانی در v2.3.0.

> **قاعده اجرا:** این اسکریپت بین «آنچه اکنون در v2.2.0 پیاده‌سازی شده» و «آنچه در نقشه راه v2.3.0 قرار دارد» تفاوت روشن می‌گذارد. درباره SSO، MFA واقعی، production Plaid یا انطباق قانونی، نباید ادعای تکمیل‌شده مطرح شود مگر با شواهد قراردادی و فنی قابل ارائه.

## نقشه زمان‌بندی

| اسلاید | زمان هدف | پیام مرکزی |
| --- | ---: | --- |
| 1. جلد | 0:00 تا 0:40 | امنیت را به یک تصمیم کسب‌وکاری و پیش‌نیاز Enterprise تبدیل کنید. |
| 2. چرایی اکنون | 0:40 تا 2:00 | با عملیاتی‌شدن داده بانک و گزارش، سطح کنترل باید ارتقا یابد. |
| 3. رد پیش‌فرض | 2:00 تا 3:35 | مجوز در مرز سرویس و در محدوده شرکت اعمال می‌شود. |
| 4. عملیات حساس | 3:35 تا 5:10 | بانک و گزارش بیرونی با کنترل‌های ویژه محافظت می‌شوند. |
| 5. DPAPI | 5:10 تا 6:45 | کلید محلی ویندوز از فایل خام به حفاظت وابسته به پروفایل منتقل شده است. |
| 6. شواهد و محدودیت‌ها | 6:45 تا 8:35 | کنترل‌ها آزمون شده‌اند و گام‌های باقیمانده شفاف‌اند. |
| 7. تصمیم‌ها | 8:35 تا 10:15 | هیئت‌مدیره باید سه تصمیم اجرایی اتخاذ کند. |

---

## اسلاید 1 — FinAnalyzer Enterprise v2.2.0

**زمان پیشنهادی:** ۴۰ ثانیه

### متن گفتاری دقیق

«موضوع امروز، صرفاً اضافه‌شدن چند قابلیت امنیتی به FinAnalyzer نیست. موضوع این است که با تبدیل‌شدن محصول از یک محیط تحلیل مالی به پلتفرمی که دفتر کل، اتصال بانکی و گزارش‌گیری عملیاتی را کنار هم قرار می‌دهد، امنیت باید به بخشی از معماری و مدل فروش Enterprise تبدیل شود.

در v2.2.0، ما یک پایه مشخص ایجاد کرده‌ایم: کنترل دسترسی سازمانی در سطح سرویس، تفکیک داده بر اساس شرکت، ثبت رویدادهای حساس و حفاظت بهتر از کلید رمزنگاری محلی در ویندوز. هدف این ارائه سه‌گانه است: آنچه تحویل داده شده را روشن کنیم، محدودیت‌های باقی‌مانده را پنهان نکنیم، و تصمیم‌هایی را که برای کامل‌کردن هویت سازمانی در v2.3.0 نیاز داریم مطرح کنیم.»

### مکث و تأکید

پس از عبارت «مدل فروش Enterprise» مکث کوتاه کنید. در جمله آخر با دست به سه بخش اصلی «تحویل داده شده»، «محدودیت‌ها» و «تصمیم‌ها» اشاره کنید.

### انتقال

«ابتدا باید روشن کنیم چرا این سرمایه‌گذاری امنیتی در این مقطع ضروری است.»

---

## اسلاید 2 — چرا این ارتقا اکنون ضروری است

**زمان پیشنهادی:** ۸۰ ثانیه

### متن گفتاری دقیق

«سه جریان در FinAnalyzer به هم رسیده‌اند. نخست، داده مالی و دفتر کل که مبنای تصمیم‌های مدیریتی است. دوم، اتصال بانکی مبتنی بر رضایت، که داده را از سطح فایل و import دستی به چرخه عملیاتی نزدیک می‌کند. سوم، گزارش‌های PDF و Excel زمان‌بندی‌شده، که امکان رساندن داده به ذی‌نفعان بیرونی یا داخلی را فراهم می‌کنند.

هر کدام از این سه جریان به‌تنهایی ارزشمند است، اما در کنار هم سطح مسئولیت محصول را افزایش می‌دهند. دیگر کافی نیست که یک دکمه در رابط کاربری فقط برای مدیر نمایش داده شود. باید مطمئن شویم شخصی که عملیات را شروع می‌کند واقعاً کاربر فعال است، واقعاً عضو همان شرکت است، واقعاً مجوز آن عمل را دارد، و برای عملیات بسیار حساس احراز هویت قوی‌تری انجام داده است.

بنابراین، این ارتقا هزینه زیباسازی فنی نیست. این پایه‌ای است برای اینکه بتوانیم با مشتری سازمانی درباره اتصال داده، کنترل داخلی و اعتماد عملیاتی گفت‌وگوی معتبر داشته باشیم.»

### مکث و تأکید

روی عبارت «فقط پنهان‌کردن دکمه کافی نیست» مکث کنید. هدف این است که تفاوت UI با کنترل واقعی در ذهن هیئت‌مدیره تثبیت شود.

### انتقال

«پاسخ ما به این مسئله، یک زنجیره ساده اما سخت‌گیرانه برای تصمیم‌گیری مجوز است.»

---

## اسلاید 3 — رد پیش‌فرض؛ مجوز فقط با اثبات صریح

**زمان پیشنهادی:** ۹۵ ثانیه

### متن گفتاری دقیق

«هسته v2.2.0، سرویس مرکزی AuthorizationService است. منطق آن روشن است: هیچ عملیات محافظت‌شده‌ای به‌صورت پیش‌فرض مجاز نیست. درخواست باید از پنج ایستگاه عبور کند.

ایستگاه اول، کاربر فعال است. حساب غیرفعال یا ناشناخته، صرف‌نظر از ظاهر رابط، نمی‌تواند عمل مالی انجام دهد. ایستگاه دوم، عضویت معتبر در شرکت است. کاربر ممکن است در شرکت A نقش داشته باشد، اما این حق به شرکت B منتقل نمی‌شود. ایستگاه سوم، نقش سازمانی است؛ برای نمونه analyst، accountant یا finance manager. ایستگاه چهارم، permission صریح مثل bank.sync یا report.deliver.external است. و ایستگاه پنجم، اجرای عمل در خود سرویس است.

این ساختار دو مزیت دارد. اول، تفکیک tenant را واقعی می‌کند: شناسه یک گزارش، حساب بانکی یا شرکت دیگر نباید به کاربر راهی برای دسترسی افقی بدهد. دوم، کنترل را از رابط کاربری مستقل می‌کند. حتی اگر کسی تلاش کند مستقیم connector یا script را فراخوانی کند، سرویس باید همان policy را کنترل کند.

همه درخواست‌های ردشده و مجوزهای حساس در audit trail ثبت می‌شوند. این برای تشخیص سوءاستفاده، بررسی رخداد و نشان‌دادن کنترل به مشتری سازمانی ضروری است.»

### مکث و تأکید

وقتی از پنج ایستگاه نام می‌برید، به ترتیب روی نمودار حرکت کنید. بعد از جمله «شرکت A به B منتقل نمی‌شود» مکث کوتاه کنید تا مفهوم tenant isolation روشن شود.

### انتقال

«حال ببینیم این منطق، در چهار عمل مالی که بیشترین حساسیت را دارند، چگونه استفاده شده است.»

---

## اسلاید 4 — کنترل‌های ویژه عملیات مالی حساس

**زمان پیشنهادی:** ۹۵ ثانیه

### متن گفتاری دقیق

«در نخستین ردیف، اتصال بانک با Plaid را می‌بینید. این عمل فقط با permission مشخص bank.link و context MFA انجام می‌شود. یعنی محصول باید بداند چه کسی اتصال را آغاز کرده و برای این عمل حساس، احراز هویت تقویت‌شده وجود داشته است.

در ردیف دوم، همگام‌سازی تراکنش است. این عملیات permission جداگانه bank.sync دارد و فقط در محدوده همان شرکت مالک Item اجرا می‌شود. در کنار آن، منطق sync از cursor اتمیک و نگاشت تراکنش به ثبت دفتر کل استفاده می‌کند تا مسیر داده قابل‌ردیابی بماند.

ردیف سوم، تولید گزارش است. گزارش از دفتر کل همان شرکت تولید می‌شود و سیاست خروجی باید تضمین کند که توکن بانکی، کلید رمزنگاری یا credential ارسال داخل PDF و Excel قرار نمی‌گیرد.

و در نهایت، ارسال بیرونی گزارش است. این نقطه یک مرز کنترل داخلی است، چون داده می‌تواند از محیط سازمان خارج شود. بنابراین، permission حساس report.deliver.external و MFA context لازم است. در استقرارهای حساس‌تر، همین‌جا می‌توانیم allowlist گیرندگان و تأیید دومرحله‌ای یا maker-checker را هم اضافه کنیم.

پیام این اسلاید ساده است: هر عمل حساس، مجوز مشخص و کنترل متناسب با ریسک خود دارد.»

### مکث و تأکید

عبارت «داده می‌تواند از محیط سازمان خارج شود» را آهسته‌تر بیان کنید. این جمله، چرایی کنترل delivery را برای اعضای غیرفنی روشن می‌کند.

### انتقال

«علاوه بر کنترل اینکه چه کسی چه کاری انجام می‌دهد، باید از کلیدی که اسرار محلی را حفاظت می‌کند نیز مراقبت کنیم.»

---

## اسلاید 5 — Windows DPAPI؛ حذف فایل کلید خام

**زمان پیشنهادی:** ۹۵ ثانیه

### متن گفتاری دقیق

«در نسخه‌های قبلی، اگر کلید سازمانی بیرونی تعریف نمی‌شد، کلید Fernet می‌توانست به‌صورت فایل محلی نگهداری شود. در v2.2.0، رفتار ویندوز تغییر کرده است. کلید Fernet دیگر نباید به‌صورت raw key روی دیسک باقی بماند؛ این کلید با Windows DPAPI و در context پروفایل همان کاربر محافظت می‌شود.

در سمت راست، مسیر را می‌بینید: کلید Fernet برای رمزکردن اسرار یکپارچه‌سازی استفاده می‌شود؛ DPAPI آن کلید را تحت پروفایل Windows محافظت می‌کند؛ فایل محلی فقط نسخه .dpapi را نگه می‌دارد؛ و در SQLite نیز access token Plaid به‌شکل ciphertext ذخیره می‌شود.

در سمت چپ، migration یک‌باره از نسخه قبلی قرار دارد. کلید legacy خوانده می‌شود، نسخه محافظت‌شده با نوشتن اتمیک ساخته می‌شود و پس از موفقیت، نسخه خام حذف می‌گردد. نکته مهم این است که رفتار سیستم fail-closed است: اگر DPAPI نتواند کلید را بازیابی کند، برنامه نباید بی‌صدا به فایل کلید خام بازگردد.

این کنترل به‌تنهایی امنیت کامل ایجاد نمی‌کند؛ اما سطح حمله را کاهش می‌دهد و کمک می‌کند سرقت صرف دیتابیس برای استفاده از توکن بانکی کافی نباشد. در محیط‌های shared workstation یا نیاز به recovery سازمانی، گام بعدی استفاده از KMS یا سیاست machine scope کنترل‌شده خواهد بود.»

### مکث و تأکید

روی «fail-closed» و «به‌تنهایی امنیت کامل ایجاد نمی‌کند» تأکید کنید. شفافیت درباره محدودیت‌ها، اعتماد هیئت‌مدیره را افزایش می‌دهد.

### انتقال

«اکنون می‌خواهم به جای ادعای کلی، شواهد اجرا و همچنین بخش‌های تکمیل‌نشده را روشن کنم.»

---

## اسلاید 6 — شواهد اجرا و کیفیت کنترل

**زمان پیشنهادی:** ۱۱۰ ثانیه

### متن گفتاری دقیق

«برای v2.2.0، هفت آزمون یکپارچه با موفقیت اجرا شده‌اند. این عدد به‌خودی‌خود معیار امنیت نیست؛ ارزش آن در دامنه سناریوهایی است که پوشش می‌دهد.

نخست، رد پیش‌فرض آزمایش شده است: کاربری که مجوز صریح ندارد، برای عملیات محافظت‌شده رد می‌شود و event حسابرسی ثبت می‌گردد. دوم، تفکیک شرکت آزمایش شده است: نقش یا مجوز در شرکت A نباید امکان عمل روی دارایی شرکت B را ایجاد کند. سوم، MFA context آزمایش شده است: عمل حساس بانکی بدون context معتبر اجرا نمی‌شود.

در کنار آن، رفتار DPAPI و migration key، رمزشدن توکن Plaid، توازن ثبت‌های دفتر کل در sync و تولید گزارش زمان‌بندی‌شده با actor مجاز نیز بررسی شده‌اند.

اما در همین اسلاید عمداً موارد باقی‌مانده را هم نمایش داده‌ایم. SSO مبتنی بر OIDC/SAML، MFA واقعی به جای flag داخلی، service accountهای حداقل‌دسترسی و تست نفوذ مستقل هنوز کار آینده هستند. این‌ها پنهان‌کردنی نیستند؛ دقیقاً همان مواردی هستند که v2.3.0 باید با مالک، بودجه و معیار پذیرش مشخص تحویل دهد.

تعبیر درست از وضعیت امروز این است: پایه کنترل‌ها ساخته و قابل‌آزمون شده است، اما برای رسیدن به سطح IAM کامل سازمانی، به اجرای مرحله بعد نیاز داریم.»

### مکث و تأکید

بعد از جمله «این عدد به‌خودی‌خود معیار امنیت نیست» مکث کنید. این جمله مانع برداشت اشتباه از تعداد testها می‌شود و حرفه‌ای‌بودن رویکرد را نشان می‌دهد.

### انتقال

«با این تصویر، سه تصمیم مشخص از هیئت‌مدیره می‌خواهیم.»

---

## اسلاید 7 — تصمیم‌های پیشنهادی هیئت‌مدیره

**زمان پیشنهادی:** ۱۰۰ ثانیه

### متن گفتاری دقیق

«تصمیم اول، پذیرش v2.2.0 به‌عنوان پایه امنیتی محصول Enterprise است. این یعنی از این پس، قابلیت‌های جدید مالی و یکپارچه‌سازی‌ها باید از authorization سرویس‌محور، tenant scope و audit trail استفاده کنند؛ امنیت نباید افزونه‌ای پس از توسعه باشد.

تصمیم دوم، اولویت‌بندی IAM سازمانی در v2.3.0 است. پیشنهاد ما روشن است: OIDC یا SAML برای SSO، ترجیحاً از مسیر Microsoft Entra برای سازمان‌های Microsoft، MFA واقعی و مقاوم‌تر در برابر فیشینگ برای عملیات حساس، و service accountهای حداقل‌دسترسی برای jobهای زمان‌بندی‌شده. این کار از ارسال flag MFA قابل‌تغییر در داخل برنامه به session context اعتبارسنجی‌شده منتقل می‌شویم.

تصمیم سوم، الزام assurance مستقل پیش از استقرار حساس یا فعال‌سازی Plaid Production است. این شامل threat model، تست مجوز و MFA، اسکن dependency و secrets، آزمایش پایداری در outage IdP و در نهایت تست نفوذ مستقل با محیط ایزوله و Rules of Engagement مصوب است.

درخواست ما از هیئت‌مدیره، تأیید بودجه، مالک اجرایی و نقاط عطف برای این سه تصمیم است. با این حمایت، می‌توانیم FinAnalyzer را نه فقط از نظر قابلیت مالی، بلکه از نظر کنترل‌پذیری و قابلیت‌اعتماد سازمانی نیز به سطح مورد انتظار مشتریان حساس به داده نزدیک کنیم.

سپاسگزارم. خوشحال می‌شوم درباره نقشه راه v2.3.0، سطح ریسک قابل‌قبول و اولویت‌بندی سرمایه‌گذاری پاسخ دهم.»

### پایان ارائه

سه تا پنج ثانیه سکوت کنید و سپس وارد پرسش‌وپاسخ شوید. از بازگشت به اسلاید 6 برای پاسخ پرسش‌های مربوط به محدودیت‌ها و اسلاید 4 برای پاسخ پرسش‌های عملیاتی استفاده کنید.

---

# راهنمای پرسش‌وپاسخ هیئت‌مدیره

| پرسش محتمل | پاسخ پیشنهادی |
| --- | --- |
| آیا v2.2.0 به معنی آماده‌بودن کامل برای همه مشتریان Enterprise است؟ | «خیر. v2.2.0 پایه RBAC و حفاظت کلید را پیاده کرده است. SSO، MFA واقعی، service account و تست نفوذ مستقل همچنان شرط تکمیل استقرار حساس‌اند.» |
| چرا به جای اتصال مستقیم به Active Directory، Entra/OIDC پیشنهاد می‌شود؟ | «کلاینت دسکتاپ نباید credential AD یا منطق LDAP را در خود نگه دارد. OIDC مسیر استانداردتری برای token، MFA، lifecycle و سیاست‌های سازمانی فراهم می‌کند؛ AD محلی نیز می‌تواند در پشت hybrid identity باقی بماند.» |
| آیا MFA باعث اصطکاک برای تیم مالی نمی‌شود؟ | «برای مشاهده عادی، سیاست می‌تواند سبک‌تر باشد. MFA تازه یا step-up را فقط به اعمالی مانند اتصال بانک، تغییر نقش، ارسال خارجی گزارش و ثبت حساس محدود می‌کنیم.» |
| آیا DPAPI از تمام ریسک‌های سرقت داده جلوگیری می‌کند؟ | «خیر. DPAPI حفاظت محلی کلید را بهتر می‌کند، اما جایگزین endpoint protection، کنترل نشست، backup policy، KMS سازمانی یا تست نفوذ نیست.» |
| مهم‌ترین ریسک باقی‌مانده چیست؟ | «تا زمانی که MFA واقعی و SSO پیاده نشده، context MFA در سطح معماری کامل هویت ریشه ندارد. به همین دلیل، این بخش باید اولویت v2.3.0 باشد.» |
| معیار خروج از pilot چیست؟ | «اعتبارسنجی OIDC/PKCE و step-up، تست tenant isolation و revoke، تست Windows package، اسکن dependency/secrets، و Pentest مستقل با یافته‌های بحرانی بسته یا پذیرفته‌شده به‌صورت رسمی.» |

# چک‌لیست پیش از جلسه

پیش از ارائه، از branch و نسخه‌ای استفاده کنید که واقعاً مورد بحث است. تنها از داده‌های مصنوعی برای دموی Plaid یا گزارش استفاده کنید. اگر عضو هیئت‌مدیره درباره release یا production readiness پرسید، میان «کد پیاده‌سازی‌شده»، «آزمون داخلی پاس‌شده» و «تأیید مستقل» تفاوت صریح بگذارید. برای هر پرسش درباره هزینه، زمان یا تعهد قراردادی، فقط عددی را بیان کنید که میان تیم مؤسس و مسئولان اجرایی تأیید شده باشد.
